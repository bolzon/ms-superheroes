sudo: required
language: node_js
nodejs:
  - "8"
  - "10"
services:
  - docker
before_install:
  - sudo /etc/init.d/mysql stop
install:
  - npm run docker:install
  - npm run docker:start
  - npm install -g mocha@5.2.x
  - npm install
cache:
  directories:
    - "node_modules"
