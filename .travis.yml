sudo: required
dist: xenial
language: node_js
nodejs:
  - "8"
  - "10"
env:
  - DB_TEST=true
  - CXX=g++-4.8
addons:
  apt:
    sources:
      - ubuntu-toolchain-r-test
    packages:
      - g++-4.8
before_install:
  - npm install -g mocha@5.2.x nyc@13.0.x
install:
  - npm install
script:
  - npm test
cache:
  directories:
    - "node_modules"
