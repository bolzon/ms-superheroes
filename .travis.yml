sudo: required
language: node_js
nodejs:
  - "8"
  - "10"
services:
  - docker
env:
  - DB_TEST=true
before_install:
  - sudo /etc/init.d/mysql stop
  - npm install -g mocha@5.2.x
  - npm install -g nyc@13.0.x
install:
  - npm install
script:
  - npm test
cache:
  directories:
    - "node_modules"
